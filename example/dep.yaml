api:
- endpoint: "http://localhost:80"
  statuscode: 200
  timeout: 1000
database:
    postgres:
    - conn: "user=user password=pass dbname=mydb host=localhost port=5432 sslmode=disable"
    redis:
    - conn: "localhost:6379"
grpc:
  - host: "localhost:4770"
    timeout: 1000

        